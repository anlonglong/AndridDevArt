this is readme file to intro this project

add second text

add third text

add fourth text

add from master branch
add text at dev branch

add text from aaa branch


想要删除当前所在的分支的话,必须切换到其他分支,因为在当前分支下面无法删除自己


今天是元旦的假期的第一天,这句话是下午13:48写的


fixed issis-01
 fixed issis-02
add text in NIO
添加revert的用法,revert虽然是撤销上一次的提交,但是会新生成个cimmitID,也是头指针会前移一位,而reset的操
则是头指针会往后面移动,不会生成新的commitId,也就是不会有历史记录在里面的文件夹.

revert使用的场合:把文件已经push到服务器的时候,可以使用revert把提交到服务器的文件会退之前的状态.

6666666


add text in NIO

